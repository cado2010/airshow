node_modules/
dist/
*.log
logs/
*.local
.env
.DS_Store
app/dist/
server/dist/

# Electron build outputs
release/
electron/server.cjs
build/

# Secrets (OpenSky OAuth client credentials) — bundled into the local build but never committed
server/creds/
