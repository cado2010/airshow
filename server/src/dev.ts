import { startServer } from "./index.js";

// Standalone / dev entry point: start the API server on PORT (default 8787).
// The Vite dev server proxies (and the browser connects directly) to it.
void startServer();
